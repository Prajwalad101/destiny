import Checkbox from './Checkbox';

export default {
  title: 'components/Checkbox',
  component: Checkbox,
};

const Template = () => <Checkbox>Custom Box</Checkbox>;

export const Base = Template.bind({});
